# 参与 MathProblemBank

当前接受范围仅限 math v0.1：数学题库、学习项目、LaTeX/PDF、数学词汇、数学 AI 助手与网课讲义工作流。物理、英语和尚未经过长期自用验证的工作区暂不作为公开功能维护。

提交问题前请删除日志中的 API key、Cookie、用户名、绝对路径、教材内容和真实数据库数据。能够复现问题时，优先使用空白用户数据目录和自行编写的最小数学样例。

代码修改应满足：

- 不改变现有数据库或文件格式，除非同时提供兼容迁移和恢复说明；
- 不把数据库、PDF、录课、缓存、导出包或个人配置加入 Git；
- 新增可重复业务操作时复用现有执行器与 AI 操作注册表；
- 至少运行 `python -m unittest shared.scripts.test_release_engineering`；
- 与正式功能有关的修改还应运行相应聚焦测试，并说明尚未验证的外部依赖。

公开仓库应从白名单发行器生成的目录建立，不接受把私人开发仓库历史直接公开。
